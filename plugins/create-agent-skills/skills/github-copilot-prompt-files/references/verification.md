# Verification

Parse YAML; verify unique name, scalar description/hint, list tools, resolvable
agent/model, consumed inputs, relative links, and no secrets. Run the command
with complete, missing, and malformed inputs. Compare workspace snapshots and
inspect Agent Debug Logs when discovery fails.
