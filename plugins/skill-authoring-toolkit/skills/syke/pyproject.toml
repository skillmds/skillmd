[build-system]
requires = ["setuptools>=75.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "syke"
version = "0.5.10"
description = "Local-first agentic memory for AI tools, powered by the Pi runtime."
readme = "README.md"
license = "AGPL-3.0-only"
requires-python = ">=3.12"
authors = [{ name = "Utkarsh Saxena", email = "utkarsh@mysyke.com" }]
keywords = ["ai", "agent", "memory", "local-first", "identity", "cli", "pi"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "click>=8.1",
    "httpx>=0.27",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "rich>=13.0",
    "python-dotenv>=1.0",
    "uuid7>=0.1.0",
    "simple-term-menu>=1.6",
]

[project.optional-dependencies]
gmail = ["google-auth-oauthlib>=1.0", "google-api-python-client>=2.100"]
dev = ["pytest>=8.0", "ruff>=0.8", "build>=1.0"]
all = ["syke[gmail]", "syke[dev]"]

[project.urls]
Repository = "https://github.com/saxenauts/syke"
Documentation = "https://github.com/saxenauts/syke/tree/main/docs"
Changelog = "https://github.com/saxenauts/syke/blob/main/CHANGELOG.md"

[project.scripts]
syke = "syke.entrypoint:cli"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["syke*"]

[tool.setuptools.package-data]
syke = [
    "py.typed",
    "distribution/*.md",
    "llm/backends/skills/*.md",
    "observe/seeds/*.md",
    "runtime/web/*.html",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "--ignore-glob=*.calls.py --ignore-glob=*.deps.py --ignore-glob=*.impact.py"

[tool.coverage.run]
source = ["syke"]
omit = ["syke/observe/seeds/*"]

[tool.pyrefly]
search-path = ["."]
python-interpreter-path = ".venv/bin/python"

[tool.tbump]

[tool.tbump.version]
current = "0.5.10"
regex = '''(?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)'''

[tool.tbump.git]
tag_template = "v{new_version}"
message_template = "Release v{new_version}"

[[tool.tbump.file]]
src = "pyproject.toml"
search = 'version = "{current_version}"'

[[tool.tbump.file]]
src = "syke/__init__.py"
search = '__version__ = "{current_version}"'

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["E501"]
"syke/db.py" = ["E501"]
"syke/distribution/context_files.py" = ["E501"]
"syke/llm/env.py" = ["E501"]
"syke/models.py" = ["E501"]
"syke/sync.py" = ["E501"]
"syke/daemon/daemon.py" = ["E501"]
"syke/runtime/psyche_md.py" = ["E501"]
